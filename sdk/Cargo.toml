[package]
name = "sdk"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = { version = "2.11", default-features = false }
defmt = { version = "1.1", optional = true }

[build-dependencies]
cbindgen = "0.29"
