[package]
name = "sdk"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = { version = "2.11", default-features = false, features = [ "bytemuck" ] }
defmt = { version = "1.1", optional = true }
num_enum = { version = "0.7.6", default-features = false }
bytemuck = { version = "1.25", features = [ "derive" ] }

[build-dependencies]
cbindgen = "0.29"
